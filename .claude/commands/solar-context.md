# Solar UI — Chargement du contexte

Lis le manifeste du design system :

```
/Users/melvinensellem/Documents/Daily UI/.claude/knowledge/figma.json
```

Puis, selon le besoin, lis les fichiers pointés :
- **Foundations** : `.claude/knowledge/foundation/{colors,typography,spacing,elevation,assets}.json`
- **Composants** : `.claude/knowledge/components/{action,form,feedback,layout,content,data}.json`

Après lecture du manifeste, résume en une phrase le système (3 dimensions Theme/Mode/Screen + catégories de composants) et confirme que le contexte est chargé.

Ce contexte pilote toutes les décisions de design : choix de composants, variantes, tokens couleur sémantiques (Palette/*), styles typographiques et échelle d'espacement.
